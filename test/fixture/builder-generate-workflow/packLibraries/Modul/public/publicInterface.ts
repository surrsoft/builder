'use strict';

interface IMyInterfaceOptions {
   startFilter: object;
}

function testFunction(variable1, variable2) {
   if ((variable1 && variable2) {
      return variable2;
   } else {
      return variable1;
   }
}

export interface IMyInterface;